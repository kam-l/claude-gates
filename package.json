{
  "name": "claude-gates",
  "version": "3.6.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && npx c8 --reporter=text node scripts/pipeline-test.js",
    "test:e2e": "npm run build && npx c8 --reporter=text --clean=false node scripts/test-pipeline-e2e.js",
    "test:all": "npm run test && npm run test:e2e"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "langfuse": "^3.38.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.5.0",
    "better-sqlite3": "^12.8.0",
    "c8": "^10.1.3",
    "typescript": "^6.0.2"
  }
}
